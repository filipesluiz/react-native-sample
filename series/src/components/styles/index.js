import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    cardContainer:{
        flex:1
    },
    card:{
        flex:1,
        borderWidth:1   
    }

});

export default styles;
